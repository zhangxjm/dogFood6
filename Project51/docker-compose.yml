version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: nft-audit-redis
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - nft-audit-net

volumes:
  redis_data:

networks:
  nft-audit-net:
    driver: bridge
