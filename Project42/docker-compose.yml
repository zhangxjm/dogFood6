version: '3.8'

services:
  study-platform:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./backend/uploads:/app/backend/uploads
      - ./backend/study_platform.db:/app/backend/study_platform.db
    restart: unless-stopped
