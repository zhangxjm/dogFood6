version: '3.8'

services:
  nursery-app:
    build: .
    container_name: nursery-management
    ports:
      - "8080:8080"
    volumes:
      - ./nursery.db:/app/nursery.db
    restart: unless-stopped
    environment:
      - GIN_MODE=release
